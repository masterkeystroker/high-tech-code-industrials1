<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ColorPicker="clr-namespace:CustomWPFColorPicker;assembly=CustomWPFColorPicker"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" mc:Ignorable="d" x:Class="Test.MainWindow"
        Title="MainWindow" Height="1280" Width="768">
    <Grid Background="#FF646464">
    	<Grid.ColumnDefinitions>
    		<ColumnDefinition Width="0.14*"/>
    		<ColumnDefinition Width="0.162*"/>
    		<ColumnDefinition Width="0.698*"/>
    	</Grid.ColumnDefinitions>
    	<Grid.RowDefinitions>
    		<RowDefinition Height="0.093*"/>
    		<RowDefinition Height="0.907*"/>
    	</Grid.RowDefinitions>
        <Grid Grid.Column="1" Margin="17,12,16,24" DataContext="{Binding classTest}">
    		<ColorPicker:ColorPickerControlView x:Name="ForeColorPicker" CurrentColorString="{Binding StrTestA, Mode=TwoWay}"  CurrentColor="{Binding TestColorA, Mode=TwoWay}"/>
			
    	</Grid> 
		<TextBlock Text="{Binding classTest.StrTestA}" Foreground="{Binding ClassTest.TestColorA}" Grid.Column="1" Margin="0" Grid.Row="1" TextAlignment="Center" x:Name="TextBlock" HorizontalAlignment="Center" VerticalAlignment="Top" />
        <Button Content="Button" Grid.Column="2" Height="72" Background="{Binding CurrentColor, ElementName=ForeColorPicker}" HorizontalAlignment="Left" Margin="86,24,0,0" x:Name="button1" VerticalAlignment="Top" Width="220" Click="button1_Click" />
    </Grid>
</Window>
