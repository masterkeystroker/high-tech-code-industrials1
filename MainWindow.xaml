<Window x:Class="CC3_Mgr.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:CC3_Mgr"
        Title="MainWindow" Height="2" Width="2" Background="{x:Null}" Foreground="{x:Null}" WindowStyle="None" ResizeMode="NoResize" WindowStartupLocation="CenterScreen" HorizontalAlignment="Center" VerticalAlignment="Center">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <!--<local:NotifyIcon
			x:Name="notifyIcon"
			Text="{Binding ElementName=tipText,Path=Text}"
			Icon="IconCC3.ico"
			BalloonTipText="{Binding Text, ElementName=balloonTipText}"
			BalloonTipTitle="{Binding Text, ElementName=balloonTipTitle}"
			BalloonTipIcon="Info"
			MouseDoubleClick="OnNotifyIconDoubleClick">
             <local:NotifyIcon.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Open Interface" Click="OnOpenClick" />
                    <MenuItem Header="Exit" Click="OnExitClick" />
                </ContextMenu>
            </local:NotifyIcon.ContextMenu>
        </local:NotifyIcon> -->
    </Grid>
</Window>
